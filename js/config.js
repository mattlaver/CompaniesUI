'use strict';

var Config = (function() {

    var domain = 'http://localhost:222'

    var companiesUrl = '/companies';

    var api = {
        companiesUrl: domain + companiesUrl
    }

    return api;
})();